import { ChatContainer } from "@/components/ChatContainer.tsx"

function App() {
	return (
		<main className={"min-h-screen bg-[#072E6A] flex flex-col"}>
			<ChatContainer />
		</main>
	)
}

export default App
