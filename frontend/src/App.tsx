import {ChatContainer} from "@/components/ChatContainer.tsx";

function App() {
	return <main className={"min-h-screen bg-[#072E6A] flex items-center justify-center"}>
        <ChatContainer />
    </main>
}

export default App
